TRUNCATE `accommodation_inquiries`;
TRUNCATE `booking_dates`;
TRUNCATE `business_hours`;
TRUNCATE `customers`;
TRUNCATE `excursion_inquiries`;
TRUNCATE `experiences`;
TRUNCATE `faq_questions`;
TRUNCATE `faq_sections`;
TRUNCATE `inquiries`;
TRUNCATE `orders`;
TRUNCATE `order_transactions`;
TRUNCATE `password_resets`;
TRUNCATE `password_reset_tokens`;
TRUNCATE `personal_access_tokens`;
TRUNCATE `publish_seasons`;
TRUNCATE `reviews`;
TRUNCATE `users`;
TRUNCATE `vendors`;
TRUNCATE `vendor_accommodation_metadata`;
TRUNCATE `vendor_amenities`;
TRUNCATE `vendor_excursion_metadata`;
TRUNCATE `vendor_hours`;
TRUNCATE `vendor_inquiries`;
TRUNCATE `vendor_license_metadata`;
TRUNCATE `vendor_media_galleries`;
TRUNCATE `vendor_non_license_metadata`;
TRUNCATE `vendor_pricings`;
TRUNCATE `vendor_questionnaires`;
TRUNCATE `vendor_rooms`;
TRUNCATE `vendor_social_media`;
TRUNCATE `vendor_stripe_details`;
TRUNCATE `vendor_suggest`;
TRUNCATE `vendor_winery_metadata`;
TRUNCATE `vendor_wines`;
TRUNCATE `winery_carts`;
TRUNCATE `winery_cart_items`;
TRUNCATE `winery_inquiries`;
TRUNCATE `winery_orders`;
TRUNCATE `winery_order_transactions`;
TRUNCATE `winery_order_wines`;
TRUNCATE `winery_subscriptions`;
TRUNCATE `wine_reviews`;
