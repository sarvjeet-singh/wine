<div class="text-center mb-3">
    <input type="hidden" name="vendor_type" value="{{ $type }}">
    <button type="submit" class="btn theme-btn px-5">{{!empty($vendor) ? 'Update' : 'Save'}}</button>
</div>